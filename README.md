# 🧘‍♂️ Focus Tab

Focus Tab is a simple Chrome extension that helps you stay focused by hiding all tabs except your current one — and lets you restore them easily with a single click.

---

## 🚀 Features

- Hide all other tabs instantly, keeping only your active one visible.
- Restore all hidden tabs with one click.
- Lightweight and private — works entirely locally.
- No data collection or tracking.

---

## 🛠️ Installation (Developer Mode)

1. Download or clone this repository.
2. Open **Chrome** and go to `chrome://extensions/`.
3. Enable **Developer mode** (top-right toggle).
4. Click **Load unpacked** and select this project’s folder.
5. The **Focus Tab** icon should now appear in your Chrome toolbar.

---

## ⚙️ Permissions

| Permission | Purpose |
|-------------|----------|
| `windows` | Control the browser window to show only the focused tab. |
| `storage` | Save hidden tab information locally (if required). |

These permissions are used **only** for tab management — your data never leaves your device.

---

## 🧩 Files Overview

